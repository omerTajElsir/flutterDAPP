class DropdownItemsModel {
  const DropdownItemsModel(this.symbol, this.icon, this.address, this.decimals);
  final String symbol;
  final String icon;
  final String address;
  final int decimals;
}
